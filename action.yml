name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
  github-token:  # id of input
    description: 'github-token'
    required: true
  actions:  # id of input
    description: 'actions'
    required: true
    default: 'all'
outputs:
  result: # id of output
    description: 'Random number'
    value: ${{ steps.check-github-token.outputs.result }}
runs:
  using: "composite"
  steps:
    - name: check github token 
      id: check-github-token 
      if: ${{ inputs.who-to-greet == 'echoiitime-javascript' }}
      run: echo "::set-output name=result::$(0)"
      shell: bash
    # - name: node test
    #   id: node_test
    #   uses: echoiitime-inc/hello-world-javascript-action/.github/workflows/node_test.yml@master
    # -  uses: ./.github/workflows/workflow-2.yml
    # - uses: actions/github-script@v6
    #   id: hello-world-javascript-action
    #   with:
    #       github-token: ${{ secrets.ORG_ACCESS_TOKEN }}
    #       script: |
    #         await github.rest.actions.createWorkflowDispatch({
    #           owner: 'echoiitime-inc',
    #           repo: 'hello-world-javascript-action',
    #           workflow_id: 'node_test.yml',
    #           ref: 'master'
    #         })
    # - name: Get hello-world-javascript-action the output
    #   run: echo "The time was ${{ steps.hello-world-javascript-action.outputs.result }}"
    #   shell: bash
      
    